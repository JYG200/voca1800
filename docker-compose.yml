services:
  # 프런트엔드 (Vue 3)
  frontend:
    build: ./frontend
    container_name: voca1800-ui
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=http://host.docker.internal:8080
    volumes:
      - ./frontend:/app
      - /app/node_modules